name: phimhayokup
description: "Ứng dụng xem phim - wrapper app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  cached_network_image: ^3.3.1
  webview_flutter: ^4.7.0
  go_router: ^14.2.0
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  shimmer: ^3.0.0
  flutter_rating_bar: ^4.0.1
  url_launcher: ^6.3.0
  youtube_player_flutter: ^9.1.1
  shared_preferences: ^2.3.2
  connectivity_plus: ^6.1.4
  share_plus: ^10.0.0
  in_app_review: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  riverpod_generator: ^2.4.3
  build_runner: ^2.4.9

flutter:
  uses-material-design: true
  assets:
    - assets/images/
